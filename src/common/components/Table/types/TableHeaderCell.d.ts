import {ReactNode, ReactText} from 'react';

interface ITableHeaderCellProps {
	children: ReactNode;
	className?: string;
	width?: ReactText;
	height?: ReactText;
}

export default ITableHeaderCellProps;
