import React from 'react'

import styles from './createBuildButton.scss'

const CreateBuildButton = () => (
  <div className={styles.wrapper}>
    <div className={styles.button}>CREATE NEW BUILD</div>
  </div>
)

export default CreateBuildButton